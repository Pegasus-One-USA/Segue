# Shell scripts must always checkout with LF - CRLF breaks their shebang when copied into Linux
# containers (see containerization/docker/fhirbridge-app/entrypoint.sh) or run under bash/WSL.
*.sh text eol=lf
