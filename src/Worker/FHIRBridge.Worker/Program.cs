// Placeholder host for the worker skeleton.
// Replace with the real background-service registrations when porting the Worker.
var builder = Host.CreateApplicationBuilder(args);
var host = builder.Build();
host.Run();
