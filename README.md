# Segue
Think of Segue  as a smart translator and traffic controller for healthcare data. It sits between hospitals' medical record systems and the places where that data needs to go (like analytics tools, databases, or research platforms). It speaks different "languages" so all systems can understand each other.

## Documentation
- **[Backend Architecture Guide](docs/backend/README.md)** — how the backend works: domain & data, API surface, the runtime pipeline, and app services / building blocks. Start here for backend work.
- **[Solution Structure](STRUCTURE.md)** — project list, dependencies, and NuGet packages per project.
