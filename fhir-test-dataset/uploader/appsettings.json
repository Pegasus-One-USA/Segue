{
  "Fhir": {
    "BaseUrl": "https://segue.pegasusone.com:7011/fhir",
    "TimeoutSeconds": 120,
    "BatchSize": 20,
    "MaxRetries": 4,
    "RetryBaseDelaySeconds": 2,
    "DatasetRoot": "../../../..",
    "FailedResourceLog": "failed-resources.log",
    "StateFile": "upload-state.json"
  },
  "Auth": {
    "Comment": "Leave BearerToken empty for anonymous servers (segue HAPI is anonymous). Never hardcode credentials here; the app prefers the FHIR_BEARER_TOKEN environment variable over this value.",
    "BearerToken": ""
  }
}
